/**
 * @param conf 初始化配置
 */
Night = function(conf) {
};

/**
 * @param a first
 * @param b second
 * @returns add result
 */
Night.prototype.add = function(a, b) {
    return a + b;
};

